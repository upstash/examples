# Populated automatically by `stripe projects add upstash/redis`
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Populated automatically by `stripe projects add supabase/postgres`
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# The deployed origin (used for absolute redirect URLs in magic-link emails).
# Set in Vercel; for local dev keep http://localhost:3000.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
